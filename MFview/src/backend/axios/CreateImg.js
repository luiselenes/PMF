/*const SERVER_URL = "27.0.0.1:3000";
Vue.use(Toasted);
new Vue({
    el:"#createimg",
    data:() => ({
        imagen:{
            des:""
        },
    }),
    methods:{
        async save(){
            if(!this.imagen.des){
                return this.$toasted.show ("escriva una descripcion", {
                position:top-left,
                duration: 100,
            });
           
        }
-*--------        ponerlos demas
    const payload = JSON.stringify(this.imagen);
    const url = SERVER_URL + "/imagens"
    const r = await fetch(url ,{
        method:"POST",
        body:payload,
        headers:{
            "content-type": "application/json"
        }
    })*/
    
   
